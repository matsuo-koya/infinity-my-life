import React from "react";
import { createRoot } from "react-dom/client";
import InfinityMyLife from "./InfinityMyLife.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <InfinityMyLife />
  </React.StrictMode>
);
